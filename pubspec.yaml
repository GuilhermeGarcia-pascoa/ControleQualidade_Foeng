name: controle_qualidade_foeng
description: App de Controlo de Qualidade FOENG.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  image_picker: ^1.0.7
  shared_preferences: ^2.5.5
  intl: ^0.19.0
  path_provider: ^2.1.2
  crypto: ^3.0.3
  http: ^1.6.0
  google_fonts: ^6.2.1  # Verifica a versão mais recente, se necessário

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png